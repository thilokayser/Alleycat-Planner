# Alleycat Dispatch — Roadmap Phasen 14–23

Stand: 17.08.2026. Fasst die zehn offenen Phasen aus [`handover/alleycat-dispatch-feature-uebergabe_1.md`](handover/alleycat-dispatch-feature-uebergabe_1.md) (14–17) und [`handover/ubergabe2.md`](handover/ubergabe2.md) (18–23) neu zusammen — nicht mehr in der ursprünglichen Dokument-Reihenfolge, sondern semantisch gruppiert und nach Aufwand/Abhängigkeit sortiert. Baut direkt auf [archive/alleycat-dispatch-abgleich-status.md](archive/alleycat-dispatch-abgleich-status.md) und [archive/alleycat-dispatch-abgleich-status-teil2.md](archive/alleycat-dispatch-abgleich-status-teil2.md) auf (dort steht der Zeile-für-Zeile-Code-Abgleich, hier die Planung darauf).

---

## 1. Warum umsortiert? — Die Planungslogik

Die beiden Übergabedokumente wurden nacheinander geschrieben und nummerieren die Phasen chronologisch nach Brainstorming-Reihenfolge, nicht nach Bau-Reihenfolge. Vier Beobachtungen, die zu einer anderen Reihenfolge führen:

1. **Datei-Überschneidung vermeiden.** Phase 15 (Zonen-System) und Phase 18 (Karten-Logistik) bauen beide `map.js` und die Checkpoint-Sidebar um. Phase 17 (PDF-Baukasten 2.0) und Phase 20 (internationale Formate) bauen beide `export-pdf.js` um. Werden solche Paare getrennt nacheinander abgearbeitet, wird dieselbe Datei zweimal grundlegend angefasst — mehr Merge-Aufwand, mehr Regressionsfläche. Sinnvoller: zusammenlegen, in einem Zug bauen.
2. **Infrastruktur vor ihren Konsumenten.** Phase 16 (Feature-Registry) soll *jedes* schaltbare Feature zentral auffindbar machen. Wird sie erst gebaut, nachdem 15, 18 und 21 bereits neue Toggles (Zonen-Modi, Cargo-Modus, Trackbike-Kategorien) eingeführt haben, müssen diese nachträglich verkabelt werden. Registry zuerst spart diesen zweiten Durchgang.
3. **Aufwand/Risiko-Verhältnis.** Phase 19 hat bereits zwei funktionierende Bausteine im Code (Sidebar-Resize, Enter-Submit im Check-in) und keine Abhängigkeiten — günstiger, risikoarmer früher Gewinn. Die Zonen-Migration aus Phase 15 dagegen ist die einzige "echte" Migration unter allen zehn Phasen (bestehender, gestesteter Battle-Royale-Code muss umgebaut werden, ohne `test-suite.js` zu brechen) — bewusst mit Zeit/Testabsicherung eingeplant, nicht verdrängt.
4. **Experimentelles zuletzt.** Phase 22 (Screen-to-Camera-QR-Sync) ist technisch neuartig für dieses Projekt, ohne verlässliche Aufwandsschätzung, und keine andere Phase wartet auf sie. Gehört ans Ende, nicht weil sie unwichtig ist, sondern weil ihr Risiko am wenigsten vorhersehbar ist.

**Entscheidung (17.08.2026):** `alleycat-dispatch-feature-uebergabe_1.md`, Abschnitt 7, hält fest, dass UI-Politur bewusst erst *nach* vollständigem MVP angegangen werden soll. Phase 16 (Settings-Hub, Empty-States) und Phase 19 (Command Palette, Themes, Hover-Sync) sind im Kern genau das — UI-Politur. Bewusste Ausnahme von dieser Regel: beide Pakete werden **jetzt zuerst** gebaut, noch vor dem Backend-Fundament, weil sie der schnellste Weg zu echtem Nutzer-Testing/Feedback sind — ein spürbarer UX-Sprung, den Nutzer sofort merken, ohne dass ein Server-Setup nötig ist. Backend-Härtung (Paket 1) bringt dagegen keinen für Endnutzer sichtbaren Unterschied.

---

## 2. Roadmap: 7 aktive Arbeitspakete + 1 zurückgestellte Idee

| # | Arbeitspaket | Enthält | Aufwand | Status heute |
|---|---|---|---|---|
| **1** | **Schnelle Effizienz-Gewinne** | Phase 19 komplett | M | ✅ abgeschlossen (17.08.2026) |
| 2 | Ordnung vor mehr Features | Phase 16 komplett | M–L | ✅ abgeschlossen (17.08.2026) |
| 3 | Backend-Fundament | Phase 14 komplett | M | ⚠️ fast abgeschlossen (17.08.2026) — nur der reale Testlauf auf `hasencore.de` fehlt noch (braucht deinen Zugriff) |
| 4 | Großes Karten-Programm | Phase 15 + Phase 18 zusammengelegt | XL | ⚠️ Battle Royale existiert in alter Form (Phase 11), Rest offen |
| 5 | Export & Lokalisierung | Phase 17 + Phase 20 zusammengelegt | L | ❌ vollständig offen |
| 6 | Spezielle Rennformate | Phase 21 komplett | M–L | ⚠️ teilweise (19.08.2026) — nur `clue_sheet`; Cargo-Modul und Trackbike-Attribute wurden auf Nutzerwunsch nach dem Bauen wieder entfernt |
| 7 | Beamer-Erweiterungen | Phase 23 komplett | S–M | ❌ übersprungen (19.08.2026), auf Nutzerwunsch |
| 8 | Paket-Abholung/-Zustellung | nicht Teil der Übergabe-Doks | S | ✅ abgeschlossen (19.08.2026) — Ad-hoc-Nutzerwunsch |
| 9 | Settings-Sidebar-Redesign | nicht Teil der Übergabe-Doks | M | ✅ abgeschlossen (19.08.2026) — Ad-hoc-Nutzerwunsch |
| 10 | Auto-Backup an/ausschaltbar | nicht Teil der Übergabe-Doks | S | ✅ abgeschlossen (19.08.2026) — Ad-hoc-Nutzerwunsch |
| 11 | Fahrer-Sidebar-Redesign | nicht Teil der Übergabe-Doks | M | ✅ abgeschlossen (19.08.2026) — Ad-hoc-Nutzerwunsch |
| — | *Idee, zurückgestellt:* Offline-Gerätesync (früher Paket 8) | Phase 22 komplett | L (unsicher) | ⚠️ Kamera/QR-Grundlage (jsQR) bereits vorhanden, Stream-Protokoll komplett offen — **kein aktives Arbeitspaket**, wandert zu den "vertagten Ideen" wie Kopfgeld-Modus/Service-Worker in der ursprünglichen Übergabe |

Aufwand-Skala: S = klein (wenige, isolierte Änderungen), M = mittel (ein neues Modul oder mehrere verteilte Änderungen), L = groß (neues Modul + Umbau bestehender Kernlogik), XL = sehr groß (mehrere neue Module + Migration bestehender, produktiver Daten).

**Entscheidung 17.08.2026:** Pakete 1 und 2 (vormals 2 und 3) werden bewusst vor das Backend-Fundament gezogen — als Ausnahme von "MVP vor UI-Politur", um schnell echtes Nutzer-Feedback zu bekommen. Paket "Offline-Gerätesync" (vormals Paket 8) ist aus der aktiven Roadmap raus, bleibt als Idee vorgemerkt.

**Abhängigkeiten zwischen den Paketen:**
- Paket 2 (Registry) sollte vor Paket 4 und 6 stehen, da beide neue Toggles einführen, die die Registry sonst nachträglich aufnehmen muss.
- Paket 5 sollte vor Paket 6 stehen: der neue `clue_sheet`-PDF-Block aus Phase 21 baut sinnvoll auf dem in Phase 17 erweiterten Block-System auf (Breiten/Layout), nicht auf dem alten linearen.
- Paket 3 (Backend) ist zu allen anderen unabhängig (reines PHP-Backend) — kann parallel zu jedem anderen Paket laufen, falls Kapazität für zwei Spuren gleichzeitig da ist.
- Paket 7 ist zu allen anderen unabhängig.

**Aktuelle Bau-Reihenfolge:** 1 → 2 → 3 → 4 → 5 → 6 → 7, mit der Möglichkeit, Paket 3 jederzeit parallel einzuschieben, da es keine gemeinsamen Dateien mit den anderen Paketen berührt.

---

## 3. Detaillierte To-Do-Liste

Checkboxen markieren, was laut Code-Abgleich bereits existiert. Alles ohne `[x]` ist noch zu bauen.

### Paket 3 — Backend-Fundament (Phase 14) ⚠️ fast abgeschlossen (17.08.2026) — ein Punkt braucht dich

- [x] Prepared Statements durchgängig (`api.php`)
- [x] API-Key ≥32 Byte, zufällig
- [x] `.htaccess`-Schutz für `config.php`
- [x] `utf8mb4` als Charset gesetzt
- [x] Pre-Flight-Check-Modul (PHP-Version, Extensions, `utf8mb4`-Fallback, MySQL-Version, Schreibrechte, `max_execution_time`, `memory_limit`) + Einhängen in `install.php` vor jedem DB-Schreibvorgang
- [x] `utf8mb4`→`utf8`-Fallback-Logik (Feature-Detection statt Versionsvergleich)
- [x] `display_errors` in Produktion deaktivieren + serverseitiges Fehler-Logging statt Fehlerausgabe im Response-Body
- [x] API-Key-Hashing in der DB (`password_hash`/`password_verify`, Klartext-Key nur einmalig auf der Erfolgsseite sichtbar)
- [x] `install.php`-Selbstsperre (Selbstlöschung nach Erfolg) + Löschhinweis als Fallback
- [x] Server-seitiger Backup-Export-Endpoint (`backup.php`, JSON-Dump)
- [x] Migrations-Verifikation gegen frische + befüllte Test-DB (`migrations.php`, real gegen lokale MariaDB getestet — siehe `COMPATIBILITY.md`)
- [x] Race-Condition-Testfälle (real gegen lokale MariaDB getestet, Ergebnis + Einordnung in `COMPATIBILITY.md`)
- [x] Mini-Lasttest (100 parallele Keys, real gegen lokale MariaDB getestet)
- [x] `COMPATIBILITY.md` angelegt
- [ ] **Realer Testlauf auf `hasencore.de`** — braucht Zugriff auf deinen dortigen Webspace, kann ich nicht selbst durchführen. Sobald du `php-backend/` dort hochgeladen und `install.php` aufgerufen hast: Ergebnis (insbesondere die Pre-Flight-Check-Ausgabe) in `php-backend/COMPATIBILITY.md` nachtragen
- [x] `INSTALL.md`/README um Mindestanforderungen + "bei Rot → lokale Variante empfehlen" ergänzt

Details siehe [CLAUDE.md](CLAUDE.md), Abschnitt "Paket 3 (Phase 14): Backend-Härtung", und [php-backend/COMPATIBILITY.md](php-backend/COMPATIBILITY.md) für die konkreten Testergebnisse (inkl. einer bewusst dokumentierten, nicht in dieser Phase behobenen Grenze: Nebenläufigkeit auf Anwendungsebene bei gleichzeitiger Bearbeitung desselben Events).

### Paket 1 — Schnelle Effizienz-Gewinne (Phase 19) ✅ abgeschlossen (17.08.2026)

- [x] Resizable Sidebar mit `localStorage`-Persistenz (`map.js`)
- [x] Enter-Submit im Check-in-Startnummernfeld (Grundlage für Numpad-Workflow)
- [x] Vollständiges Einklappen der Sidebar (Toggle-Button)
- [x] Globale Tab-Shortcuts (1–6 für Navigation, Esc bricht aktive Modi/Modals ab)
- [x] Command Palette (Cmd/Ctrl+K, Fuzzy-Suche über Navigation/Fahrer/Checkpoints/Schnellaktionen)
- [x] High-Contrast Outdoor-Theme (5. Theme, `theme-outdoor`)
- [x] Hover-Sync zwischen Checkpoint-Sidebar-Zeile und Karten-Marker
- [x] Bulk-Actions (Shift-Klick-Mehrfachauswahl + Aktionsleiste) für Checkpoint-Zeilen
- [x] In-Page-PDF-Vorschau (Modal statt Direkt-Download bei Manifest/Personal-Briefing)

Details siehe [CLAUDE.md](CLAUDE.md), Abschnitt "Paket 1 (Phase 19): UI-Effizienz-Gewinne". 38 neue Checks in `test-suite.js` (361/361 bestanden), inkl. eines während der Verifikation gefundenen und behobenen echten Bugs (Leaflet-Kartengröße konnte durch einen verzögerten `invalidateSize()`-Aufruf bei ausgeblendetem Karten-Container auf 0×0 einfrieren).

### Paket 2 — Ordnung vor mehr Features (Phase 16) ✅ abgeschlossen (17.08.2026)

- [x] `src/core/feature-registry.js`: `FEATURE_REGISTRY` + `isFeatureEnabled(id, evt)`/`toggleFeature(id)`
- [x] Bestehende Features nachträglich eintragen (Kategorien, Spielmodi, Sound-Hook, Offline-Cache) — reine Verkabelung
- [x] Settings-Hub-UI (Liste, Toggle, Suche, Sprung zu Detail-Konfiguration)
- [x] `src/core/empty-states.js` (wiederverwendbare Komponente)
- [x] Empty States einbauen: Checkpoint-Liste, Fahrerliste, Leaderboard vor Rennstart, Dashboard "Letzte Aktivität" — Zonen-Editor verschoben, da das Zonen-System selbst erst mit Paket 4 entsteht
- [x] `src/core/social-share.js` (Canvas-Rendering + Web Share API mit Download-Fallback)
- [x] Social-Share-Button in der Übersicht (Status "Abgeschlossen") — Beamer-Podium-Screen folgt, sobald Paket 4 ihn liefert

Details siehe [CLAUDE.md](CLAUDE.md), Abschnitt "Paket 2 (Phase 16): Feature-Registry & Settings-Hub". 33 neue Checks in `test-suite.js` (391/391 bestanden). Scoping-Entscheidungen: `battle_royale`/`districts` fehlen bewusst in der Registry (existieren erst mit dem Zonen-System aus Paket 4); die Event-Scope-Einträge `categories`/`game_modes` sind reine, additive UI-Sichtbarkeits-Flags (`evt.featureFlags`) und lassen die bestehenden Datenmodelle (`categoryGroups`/`gameModes`) unangetastet.

### Paket 4 — Großes Karten-Programm (Phase 15 + 18)

*Teil A — Zonen-Fundament (aus Phase 15, zuerst — Phase 18 baut mit auf der aktualisierten Karten-Renderpipeline auf):*
- [x] `src/core/zones.js`: Datenmodell (Kreis + Polygon) + Geometrie-Helfer (Point-in-Circle/Polygon) + Tests (18.08.2026, 20 neue Checks in `test-suite.js`, 411/411 bestanden) — bewusst noch nicht mit `zone_active`/Battle-Royale verdrahtet, das ist der separate nächste Schritt
- [x] Leaflet.draw einbinden (bewusste Dependency-Ausnahme, dokumentiert in `PROJEKT-UEBERSICHT.md` Abschnitt 9) + Zonen-Editor-UI (18.08.2026) — Zeichnen-Toolbar (nur Kreis+Polygon) direkt auf der Karte, neues "Zonen"-Klapp-Panel in der Editor-Sidebar (Name/Farbe/Radius, Lösch- und Fokus-Icons), bidirektional synchronisiert zwischen Karte und Sidebar (`redrawZones()`/`onZoneDraw*` in `map.js`)
- [x] Bestehenden Battle-Royale-Code (`zone_active` in `game-modes.js`/`rules-engine.js`) auf das neue Zonen-System migriert (18.08.2026) — **Regressionsgefahr war real, aber abgefedert**: neues optionales `mode.config.zoneId` verweist auf eine echte `evt.zones`-Kreiszone; ohne gesetzte zoneId (jedes Bestandsevent, alle alten Tests) fällt `zoneActiveCenterOf()` exakt auf das alte `zoneCenterOf()`-Verhalten zurück — 0 Regressionen in den bestehenden Checks, 5 neue Checks für den zoneId-Pfad. Bonus: `advanceZoneStage()` synct bei jedem Stufenwechsel live den Radius der verknüpften Zone, wodurch die schrumpfende Arena jetzt tatsächlich auf der HQ-Karte und im Beamer sichtbar ist statt nur als Zahlenliste im Spielmodi-Panel
- [x] Neuer Spielmodus-Preset "Bezirke" (mehrere gleichzeitig aktive Zonen) (18.08.2026) — geteiltes Fundament mit Battle Royale, wie im Original-Briefing (`alleycat-dispatch-feature-uebergabe_1.md` §15.2) vorgesehen: `zones.js` bekam `group`/`active`-Felder + `getCheckpointZone()`; eine Zone wird per Auswahl im jeweiligen Modus-Formular automatisch `battle_royale` bzw. `district` getaggt. Zwei Sub-Varianten wie spezifiziert ("Punkte nur wenn aktiv" / "Nur erreichbar wenn aktiv"), manuelles Umschalten pro Bezirk über einen HQ-Button je Zone. Bewusst nicht gebaut (gemäß Original-Briefing als Grenzfall/Zusatz markiert, nicht Kern): Zeitplan-Umschaltung pro Bezirk, `checkpoint_zone_overrides` für manuelle Zonen-Zuordnung bei Grenzfällen — beides spätere Ergänzungen, keine Blocker
- [x] `src/core/event-locations.js`: HQ/Afterparty-Datenmodell, Checkpoint-Checkbox "ist HQ", freistehender Marker (18.08.2026) — höchstens eine Location pro `type` (`getEventLocation`/`ensureEventLocation`, kein Array-CRUD mit IDs im UI wie bei Zonen, da "mehrere Headquarters" kein sinnvoller Fall ist). HQ entweder an einen Checkpoint verknüpft (`linkedCheckpointId`, Koordinaten automatisch geerbt und bei Checkpoint-Bewegung/-Löschung nachgezogen — `syncHqLocationFromCheckpoint()` an allen Koordinaten-Änderungsstellen: Editierfelder, Marker-Drag) oder frei auf der Karte platziert (`state.locationPlacementMode` + `onMapClick`-Branch, gleiches Klick-zu-Setzen-Muster wie Checkpoints). Umschalten auf einen zweiten Checkpoint oder Überschreiben eines freistehenden Markers löst wie im Briefing gefordert einen expliziten `confirm()` aus, kein stillschweigendes Ersetzen. Löschen des verknüpften Checkpoints entfernt nur den Link, nicht die Location (bleibt an letzter bekannter Position freistehend erhalten). Neues "Sonderorte"-Klapp-Panel in der Editor-Sidebar (gleiches Muster wie das Zonen-Panel), 🏠-Badge an der Checkpoint-Zeile. 15 neue Checks in `test-suite.js`, 440/441 bestanden (einzige Abweichung: bekannter zeitabhängiger `formatMinutesAgo`-Flake, nicht durch diese Änderung verursacht). Bewusst noch nicht gebaut (folgt in den nächsten beiden Schritten): Kartenlegende mit HQ/Afterparty-Icons, PDF-Block/Dashboard-Zeile/Beamer-Einblendung, Maps-Deeplink-Button ist aber bereits pro Sonderort vorhanden.
- [x] Kartenrendering (HQ + Beamer) um Polygon-Support, Sichtbarkeits-Toggles, Legende erweitern — **plus User-Zusatzwunsch: kontinuierliches Zonen-Schrumpfen** (18.08.2026). Neue Zonen-Felder (`shrinkEnabled`/`shrinkMode`/`shrinkDurationMinutes`/`shrinkEndRadiusMeters`, kreis-only) + `effectiveZoneRadius(zone, evt, atDate)` in `zones.js`, linear zwischen `radiusMeters` (bei `evt.startConfirmedAt`) und `shrinkEndRadiusMeters` (bei `startConfirmedAt+Dauer` oder `evt.curfewTime`) interpoliert — bewusst unabhängig vom bestehenden, diskreten stufenbasierten Battle-Royale-Schrumpfen (`advanceZoneStage()`), keine Änderung an dessen Verhalten/Tests. `isPointInZone`/`getCheckpointZone` nehmen jetzt optional `evt` entgegen und werden dadurch automatisch schrumpf-aware (rückwärtskompatibel: ohne `evt` roher Radius, deckt alle Bestandsaufrufe/-tests ab). Neue Zonen-Felder `visibleOnHqMap` (Default true) und `hiddenOnBeamerUntilActive` (Default false, kombiniert mit dem bereits bestehenden `active`-Flag, jetzt generisch für jede Zone über eine neue "Aktiv"-Checkbox im Zonen-Panel schaltbar, nicht mehr nur für Bezirke). HQ-Karte (`redrawZones()`) filtert auf `visibleOnHqMap`, zeichnet Kreise mit dem live berechneten Radius und tickt alle 5s nach (`startZoneShrinkTick()`, self-stopping wie die bestehenden Countdown-Timer), plus vereinheitlichte Legende (`#map-legend-locations`/`#map-legend-zones`, HQ/Afterparty-Icons + Zonenfarben). Beamer-Zonenkarte zeichnet jetzt **alle** sichtbaren Zonen (Kreis+Polygon, live-schrumpfend) statt nur der aktuellen Battle-Royale-Stufe, und erscheint jetzt auch ganz ohne aktivierten Spielmodus (z. B. reine Bezirke oder freie Zonen) — die alte zentroid-basierte rote Stufenkreis-Darstellung bleibt als Fallback exakt für Legacy-Events ohne `mode.config.zoneId` erhalten, damit nichts doppelt gezeichnet wird. 24 neue Checks in `test-suite.js`, 464/464 bestanden.
- [x] Mobile `CollapsibleMapPanel` (Default eingeklappt <768px, `localStorage`-persistiert `alleycat:mapCollapsed`, gleiches Namensschema wie `alleycat:sidebarCollapsed`) — Ausnahme: Zonen-Editor **und** aktives "Checkpoint setzen"/Sonderort-Platzieren ignorieren den Collapse-Zustand (`isMapForceExpanded()`), nicht nur der Zonen-Editor wie im Doc-Stichpunkt, da die Karte in allen drei Fällen die Eingabefläche ist. Toggle-Leiste zwischen Karte und Sidebar (`#mobile-map-toggle`, nur sichtbar ≤820px). **Plus User-Zusatzwunsch**: Kartensuche (18.08.2026) von einem immer offenen 300px-Suchfeld oben rechts auf ein reines Lupen-Icon oben mittig-zentriert umgebaut, das per Klick das Textfeld seitlich ausfährt (`toggleMapSearch()`, Breiten-Transition, in die Esc-Prioritätskette von `handleGlobalEscape()` eingehängt) — unabhängig vom CollapsibleMapPanel selbst, aber im selben Schritt umgesetzt, da beide dieselbe Karten-Symbolleiste betreffen. 21 neue Checks in `test-suite.js` (`window.innerWidth` für die breitenabhängigen Fälle gezielt gemockt statt vom echten Testfenster abzuhängen), 479/479 bestanden (1 bekannter Timing-Flake in diesem Lauf nicht aufgetreten).
- [x] PDF-Block `event_locations`, Dashboard-Zeile Afterparty, Beamer-Afterparty-Screen (18.08.2026) — dritter Auto-Block wie `checkpoint_list` (kein manueller Inhalt, zieht HQ/Afterparty live aus `evt.eventLocations`), Dashboard-Zeile „Afterparty: [Name], [Adresse]" (`renderAfterpartyStatusLine()`, gleiches Status-Zeilen-Muster wie das bestehende Auto-Backup) mit Maps-Routen-Deeplink (`mapsDirectionsLink()`, Origin = HQ falls gesetzt sonst letzter Checkpoint nach Reihenfolge — `afterpartyRouteOrigin()`), Beamer-Einblendung „Weiter geht's bei: [Name]" nach Status „Abgeschlossen" verlinkt auf dieselbe Route. Bewusst **kein** Podium-/Siegerehrungs-Vollbildscreen gebaut — der steht laut Original-Briefing (`alleycat-dispatch-feature-uebergabe_1.md` §15.6) unter „Weitere Ergänzungen aus derselben Session", nicht im Kern-Scope von §15.1/§15.8 Schritt 7, gleiche Einordnung wie die dort bereits zurückgestellten Punkte (Zonen-Vorlagen-Export, A3-Ergebnis-Aushang, Routen-Bild-Export). **Nebenbei gefundener und behobener Bug**: zwei Stellen aus Schritt 4/5 interpolierten rohe Checkpoint-/Zonennamen in `t()`-Aufrufe, die als `innerHTML` bzw. in den (als HTML gerenderten) Beamer-Ticker landen, ohne `escapeHtml()` — `renderEventLocationRow()`s „Verknüpft mit Checkpoint"-Hinweis und `setZoneActive()`s Bezirke-Ticker-Meldungen, beide jetzt gefixt (mit Regressionstest). 21 neue Checks in `test-suite.js`, 492/492 bestanden.

*Teil B — Logistik-Intelligence (aus Phase 18, auf derselben aktualisierten Karte):*
- [x] TSP-Routen-Estimator (2-Opt-Heuristik) + Richtzeit-Rechner, Legenden-Integration (Min./Max.-Distanz) (18.08.2026) — neues `src/core/logistics.js`: Nearest-Neighbor-Starttour + 2-Opt-Verbesserung (offener Pfad, kein geschlossener Rundweg — Alleycats fahren nicht zum Start zurück), verankert am HQ falls gesetzt, sonst am Checkpoint mit `order 1`. Reine Planungshilfe: rührt `evt.checkpoints[].order`/`checkpointOrderMode` nicht an, nur ein Vergleich „aktuelle Reihenfolge" vs. „effizienteste Reihenfolge" im neuen „Routen-Schätzer"-Klapp-Panel (gleiches Muster wie Zonen/Sonderorte), inkl. editierbarer Ø-Geschwindigkeit (`localStorage`, geräte-lokal wie `alleycat:sidebarWidth`) für die Fahrzeit-Schätzung. Optimierte Route optional als eigene gestrichelte blaue Linie auf der Karte einblendbar (unterscheidbar von der echten, reihenfolge-basierten Route), mit eigenem Legenden-Eintrag. Bewusst kein Datenbank-Feld/keine Persistenz — Ergebnis ist rein `state`-lokal und wird bei jedem "Route schätzen"-Klick neu berechnet. 21 neue Checks in `test-suite.js`, 513/513 bestanden.
- [x] Proximity-Puffer-Ringe (schaltbares Layer) + Klumpen-Warnung im To-dos-Widget (18.08.2026) — in dasselbe „Logistik"-Panel aus Schritt 1 integriert (Unterüberschrift „Proximity-Puffer" statt separatem drittem Klapp-Panel, um die Sidebar nicht mit Panels zu überladen). Konfigurierbarer Puffer-Radius (Default 30 m, `localStorage`-persistiert), `findProximityClusters()` markiert alle Checkpoint-Paare innerhalb des Radius; zwei Schweregrade wie beim bestehenden Offline-Cache-Staleness-Muster (`warn` ab Radius-Unterschreitung, `danger` bei <5 m — praktisch sicher eine versehentliche Doppelplatzierung). Puffer-Ringe optional als eigener Kartenlayer einblendbar, betroffene Checkpoints rot statt grau eingefärbt statt nur als Text in der Sidebar. Klumpen-Warnung zusätzlich als neuer Eintrag in `computeDashboardTodos()`. 15 neue Checks in `test-suite.js`, 528/528 bestanden.
- [x] Logistik-Overlay: Marker-Farbbadges nach Personal-Status, Klick-zu-Sidebar-Bearbeitung (18.08.2026) — Scope bewusst reduziert (User-Entscheidung 18.08.2026): kein Material-Tracking (kein Datenmodell dafür vorhanden, verschoben), Personal-Status binär (besetzt/unbesetzt anhand `cp.staff.length`, kein Konfirmations-Flag je Person), Klick öffnet die bestehende Sidebar-Zeile statt eines neuen Karten-Popups. Kleiner farbiger Punkt oben links am Marker (`.cp-marker-staffing`, Pendant zum bestehenden Typ-Badge unten rechts), grün (`var(--ok)`) wenn `cp.staff` nicht leer, sonst rot (`var(--stamp)`, dieselbe Variable wie die Proximity-Klumpen-Warnung). Marker-Klick ruft jetzt `selectCp(cp.id)` auf statt eine eigene Mini-Version der Auswahllogik zu duplizieren — vereinheitlicht mit dem Sidebar-Zeilen-Klick und bringt `selectCp()`s neues Verhalten gratis mit: scrollt die zugehörige Sidebar-Zeile ins Blickfeld (`scrollIntoView`), nur beim Auswählen, nicht beim Abwählen. Legende um „Personal zugewiesen"/„Kein Personal" ergänzt (`#map-legend-staffing`, gleiches Muster wie Typen/Sonderorte). 6 neue Checks in `test-suite.js`, 534/534 bestanden.
- [x] Orga-Pins (`event_orga_pins`) + Rechtsklick-Kontextmenü (Checkpoint anlegen / HQ setzen / Afterparty definieren / Notiz anheften / Adresse suchen) (18.08.2026) — nach echtem Spec (`ubergabe2.md` §18.4, erst während dieses Schritts gefunden — die Roadmap-Checkliste allein hatte keine Details) umgesetzt: `evt.orgaPins = [{id, type, title, notes, lat, lng}]` in `event-locations.js` (Array-CRUD wie `zones.js`, nicht Single-per-Type wie HQ/Afterparty), bewusst **ohne** das Spec-Feld `visible_on_hq_only` — es wäre in der Praxis immer `true` gewesen (Pins laut Spec "niemals im Beamer"), ein Flag ohne echte Varianz ist totes Konfig. Rechtsklick über Leaflets eigenes natives `contextmenu`-Event (`onMapContextMenu()` in `map.js`, `e.originalEvent.preventDefault()`) statt eines weiteren Leaflet-Plugins — passt zur bestehenden "keine zusätzlichen Plugins außer der dokumentierten Leaflet.draw-Ausnahme"-Linie. Kleines selbstgebautes Floating-Menü (`#map-context-menu`, in beiden Templates ergänzt) statt Plugin-Kontextmenü; schließt sich über Klick-außerhalb (document-Listener) oder Esc (`handleGlobalEscape()`, höchste Priorität). Alle 5 Aktionen lesen `state.mapContextMenu{lat,lng}`: "Checkpoint hier anlegen" ruft eine aus `onMapClick()`s Add-Mode-Zweig herausgezogene `addCheckpointAt(lat,lng)` auf (kein duplizierter Checkpoint-Objekt-Literal mehr); "HQ setzen"/"Afterparty definieren" rufen das bestehende `placeEventLocationAt()` erneut auf; "Orga-Notiz anheften" legt sofort einen Pin an (Default-Typ `note`) und öffnet zur Bearbeitung direkt das Sidebar-Panel; "Adresse suchen" öffnet einfach die bestehende Kartensuche. Bewusst Desktop-only (kein Long-Press-Äquivalent) — Planungswerkzeug vor dem Renntag, kein Mobile-Feature. **Beim Bauen gefundener Bug**: `selectOrgaPin()`s Sidebar-Sprung fand die Zeile zunächst nicht, weil `renderOrgaPinsPanel()` (wie Zonen/Sonderorte/Logistik) im standardmäßig eingeklappten "Event-Einstellungen"-Drawer verschachtelt ist — `state.eventSettingsPanelOpen` muss mit geöffnet werden, nicht nur das innere Panel-Flag; jetzt gefixt. 25 neue Checks in `test-suite.js`, 559/559 bestanden. **Nebenbei gefunden (nicht die Ursache eines beobachteten Fehlers, aber derselbe bereits dokumentierte Bug-Klasse)**: `openEditor()`s `setTimeout(() => initMap()…, 30)` rief `initMap()` ungeschützt auf, genau wie die bereits gefixten Sidebar-Collapse/Mobile-Map-Stellen — jetzt ebenfalls auf `state.view === 'editor'` gewartet.
- [x] GeoJSON/GPX/KML-Import per Drag & Drop + 1-Klick-Checkpoint-Übernahme aus importiertem Track (18.08.2026) — neues `src/core/geo-import.js`: handgeschriebene Parser für alle drei Formate (GPX/KML via `DOMParser`, GeoJSON via `JSON.parse`, alle drei native Browser-APIs — keine neue Abhängigkeit, konsistent mit `PROJEKT-UEBERSICHT.md` §9), vereinheitlicht auf `{tracks: [[{lat,lng}]], points: [{lat,lng,name}]}`. Datei-Erkennung per Endung (`.gpx`/`.geojson`/`.json`/`.kml`), defekte/leere Dateien liefern `null` statt eines kaputten Layers. **Temporär vs. persistent** (Spec-Vorgabe): frisch importierte Layer landen in `state.geoImportLayers` (Session-only, geleert bei jedem `openEditor()` — sinnvoller Default, damit ein großer importierter Track nicht ungefragt jede künftige Speicherung des Event-JSON aufbläht); ein Häkchen "Im Event speichern" im neuen "Importierte Layer"-Panel verschiebt den Layer nach `evt.importedGeoLayers` (`setGeoImportLayerPersistent()`), `allGeoImportLayers(evt)` mergt beide Arrays fürs Rendering, sodass Aufrufer nie wissen müssen, wo ein Layer gerade lebt. **Drag & Drop**: `dragenter`/`dragover`/`dragleave`/`drop`-Listener auf `.map-wrap` (nicht nur `#map`, damit auch über Legende/Suche gedroppt werden kann), `dragCounter` löst das klassische "dragleave feuert beim Betreten eines Kind-Elements"-Flackern; Kernlogik aus dem Listener in `handleGeoImportFileDrop(file)` herausgezogen (dieselbe Begründung wie `addCheckpointAt()`s Extraktion aus `onMapClick()` in Schritt 4) — direkt mit einem echten `File`-Objekt testbar statt ein `DragEvent`/`DataTransfer` simulieren zu müssen. **Kartenrendering**: Strecken als gepunktete Linien (`dashArray:'1 6'`, dritte optisch unterscheidbare Linienart neben der echten Route und der Logistik-Routenschätzung), Wegpunkte als kleine `L.circleMarker`, deren Popup den Spec-Button "Als Checkpoint übernehmen" zeigt (`convertGeoImportPointToCheckpoint()`, ruft `addCheckpointAt()` auf und respektiert damit automatisch den bestehenden CP-Lock). 30 neue Checks in `test-suite.js`, 589/589 bestanden (1 bekannter zeitabhängiger `formatMinutesAgo`-Flake in diesem — ungewöhnlich langsamen, Background-Tab-gedrosselten — Lauf tatsächlich aufgetreten, nicht durch diese Änderung verursacht, siehe CLAUDE.md).
- [x] Clue-Vorschau-Modus (Toggle blendet Rätseltexte statt Checkpoint-Namen ein) (18.08.2026) — **letzter Teil-B-Schritt, Paket 4 damit vollständig abgeschlossen.** Reiner Anzeige-Umschalter, kein Datenmodell/keine Persistenz (`state.cluePreviewMode`, wie `state.addMode` — ein einmaliger Korrekturlese-Durchgang kurz vor dem Druck ist kein geräte-/event-weites Preference-Feature, das `localStorage` verdient hätte). Neuer 👁-Button oben links auf der Karte (`.clue-preview-toggle`, `#clue-preview-toggle`, gleiches Icon-Button-Muster wie der Such-Button, unbelegte Ecke gewählt statt Konflikt mit Suche/Zonen-Zeichenwerkzeug/Legende). Aktiv: Sidebar-Zeilen (`checkpoint.js`, `.cp-name`) zeigen `cp.clue` statt `cp.name` (kursiv, `text-transform:none` gegen die sonst großgeschriebene `.cp-name`-Basis, damit Rätseltext wie Fließtext liest statt wie ein LAUTER Titel), leerer `clue` zeigt einen kursiven "Kein Rätsel-Text hinterlegt"-Hinweis statt eines leeren Feldes. Marker-Popups (`redrawMarkers()`) werden **nur im aktiven Modus** gebunden — im Normalmodus hatten Checkpoint-Marker noch nie ein Popup (Ordnungszahl + Typ-Icon auf dem Marker selbst identifizieren den Checkpoint bereits), ein Popup dauerhaft hinzuzufügen wäre unangefordertes Extra-UI gewesen. Auffälliges Banner im Sidebar-Kopf bei aktivem Modus (`.cp-lock-banner`-Stil wiederverwendet, gleiche Warnfarbe wie der CP-Struktur-Lock-Hinweis), damit ein Organizer nicht rätselt, wohin die Checkpoint-Namen verschwunden sind. 15 neue Checks in `test-suite.js`, 604/604 bestanden (1 bekannter zeitabhängiger `formatMinutesAgo`-Flake in diesem Lauf tatsächlich aufgetreten, nicht durch diese Änderung verursacht).

### Paket 5 — Export & Lokalisierung (Phase 17 + 20)

*Teil A — Einheiten/Formate-Fundament (aus Phase 20, zuerst — Teil B nutzt diese Helfer):*
- [x] `formatDistance(meters)` zentraler Helfer mit Metrisch/Imperial-Switch, alle bestehenden Distanzstellen darauf umstellen (18.08.2026) — neu in `utils.js`, nimmt Meter entgegen (kanonische Einheit laut Spec 20.1), `state.appSettings.distanceUnit` (`'metric'|'imperial'`, geräte-lokal wie `theme`/`iconPack`, gleiches `app:settings`-Storage-Roundtrip, neuer `setDistanceUnit()`-Setter). Metrisch: <1000m → ganze Meter, sonst km mit 2 Nachkommastellen. Imperial: <528ft (≈0.1mi) → ganze Fuß, sonst Meilen mit 2 Nachkommastellen — gleiche m/km-Schwellenlogik gespiegelt auf ft/mi. Migriert: `checkpoint.js`s Checkpoint-Reihenfolge (Einzel-Etappen + Gesamtdistanz) und `logistics.js`s TSP-Routen-Schätzer (aktuelle/optimierte Distanz + Einsparung) — beide riefen vorher `.toFixed(2)} km` direkt auf. **Bewusst NICHT migriert**: editierbare Radius-Felder (Zonen-Radius, Proximity-Puffer-Radius, Shrink-Endradius) — das sind vom Organizer direkt eingetippte Meter-Schwellenwerte, keine berichtete Reisedistanz; eine bidirektionale bearbeitbare Fuß-Konvertierung wäre ein deutlich größeres Feature als dieser Schritt verlangt, siehe Kommentar in `utils.js`. `computeRouteLegs()`/`estimateOptimalRoute()` rechnen weiterhin intern in km (unverändert) — nur an den ~5 Ausgabestellen wird jetzt `formatDistance(km * 1000)` aufgerufen, statt die interne Datenrepräsentation der Berechnungsfunktionen umzubauen (rein interner Unterschied ohne Nutzer-Sichtbarkeit, hätte aber viele Call-Sites/Tests berührt). Settings-UI: neue "Einheiten"-Sektion zwischen Icon-Pack und Checkpoint-Typen, gleiches `option-card`-Muster wie Theme/Icon-Pack. 10 neue Checks in `test-suite.js`, 614/614 bestanden (1 bekannter zeitabhängiger `formatMinutesAgo`-Flake in diesem Lauf aufgetreten, nicht durch diese Änderung verursacht).
- [x] 12h/24h-Zeitformat-Switch für `formatDateTime`/`formatTimeOnly` (18.08.2026) — `state.appSettings.timeFormat` (`'24h'` Default | `'12h'`), gleiches geräte-lokales Setting-Muster wie `distanceUnit`/`theme`. 12h-Modus formatiert bewusst über `'en-US'` statt `'de-DE'` — `Intl`s `hour12`-Option respektiert zwar auch `de-DE`, würde dort aber `vorm./nachm.`-Marker statt des Spec-Beispiels "2:30 PM" liefern; das bestehende deutsche `" Uhr"`-Suffix ergibt nach einer AM/PM-Zeit ohnehin keinen Sinn mehr und entfällt im 12h-Modus. `formatDateTime()` delegiert den Zeitanteil jetzt an `formatTimeOnly()` statt die `hour12`-Verzweigung zu duplizieren. Settings-UI: "Uhrzeit"-Unterüberschrift direkt unter der neuen "Einheiten"-Sektion aus Schritt 1 (gleiches `option-card`-Muster, neue generische `.settings-subheading`-CSS-Klasse für die Unterteilung — vorher gab es nur die logistik-spezifische `.logistics-subheading`). 8 neue Checks in `test-suite.js`, 622/622 bestanden (1 bekannter zeitabhängiger `formatMinutesAgo`-Flake in diesem Lauf aufgetreten, nicht durch diese Änderung verursacht).
- [x] Koordinatenanzeige-Switch (Dezimalgrad/DMS/UTM/MGRS) (18.08.2026) — vier neue Funktionen in `utils.js`: `toDms()` (reine Gradzahl-Umrechnung, gegen das Spec-eigene Beispiel 50.9375, 6.9603 → `50°56'15"N 6°57'37"E` exakt verifiziert), `toUtm()` (Standard-Transversal-Mercator-Vorwärtsformel für WGS84 nach Snyder/USGS Professional Paper 1395 — **während der Entwicklung gegen eine echte externe Referenz validiert**, nicht nur aus dem Gedächtnis reproduziert: Wikipedias UTM-Artikel nennt den CN Tower, 43.6425667°N 79.387139°W, als 630084mE/4833438mN in Zone 17 — die eigene Implementierung trifft das auf < 1m genau), `mgrsLatBand()`/`mgrsGridSquare()`/`toMgrs()` (100km-Gitterquadrat-Buchstaben nach dem Standard-MGRS-Schema aus der UTM-Position abgeleitet, gegen dieselbe CN-Tower-Referenz auf Zone+Band+Gitterquadrat "17T PJ" exakt geprüft). `formatCoordinatesAs(format, lat, lng)` als formatexplizite Variante (für die Settings-Vorschau-Kacheln, die alle 4 Formate gleichzeitig zeigen müssen, unabhängig vom aktuell gewählten), `formatCoordinates(lat, lng)` liest `state.appSettings.coordFormat` (Default `'decimal'`). Migriert: Checkpoint-Sidebar-Readonly-Zeile (`checkpoint.js`) und alle 3 Koordinaten-Stellen im Manifest-Export (`export-pdf.js`, PDF + Web-Vorschau). **Gefundenes Problem beim Bauen**: die PDF-"Koordinaten"-Spalte war exakt für Dezimalgrad-Zeilenlänge bemessen (`flex: 0.18`) — UTM/MGRS-Strings liefen bei fester Schriftgröße Gefahr, in die Nachbarspalte zu laufen; gefixt mit einer Shrink-to-fit-Schleife (`doc.getTextWidth()` gegen die tatsächliche Spaltenbreite, Schriftgröße in 0.5pt-Schritten bis 5pt minimal reduziert). Settings-UI: dritte Unterüberschrift "Koordinatenanzeige" in der bestehenden "Einheiten"-Sektion, 4 `option-card`s, deren Beschreibungstext live eine Beispielkoordinate im jeweiligen Format zeigt (nicht nur ein Label). 20 neue Checks in `test-suite.js`, 637/637 bestanden.
- [x] Community-Sprachpakete: JSON-Import/Export in `i18n.js` + Settings-UI (18.08.2026) — **letzter Teil-A-Schritt, Teil A damit vollständig abgeschlossen.** Upload einer JSON-Datei geformt wie `translations.de` (gleiche verschachtelte Namespace/Key-Struktur), Sprachcode kommt bewusst aus dem Dateinamen (`alleycat-i18n-XX.json`, exakt das Spec-eigene Schema) statt aus einem Metadatenfeld im JSON — Export und Re-Import laufen dadurch über dieselbe Konvention. Fehlende Schlüssel in einem Sprachpaket fallen automatisch auf Deutsch zurück, weil `t()`s bestehende Pro-Key-Fallback-Logik das schon vorher konnte — ein Sprachpaket muss also nicht vollständig sein, um nutzbar zu sein. Laden/Mergen zur Laufzeit spiegelt exakt das bestehende Muster für eigene Checkpoint-Typen (`checkpointTypes:custom` in `checkpoint.js`): `loadCustomLanguagePacks()` merged aus dem neuen Storage-Key `i18n:customPacks` (geräte-lokal) direkt in das `translations`-Objekt, aufgerufen in `init()` vor `applyStaticTranslations()`. Export nutzt die bereits vorhandene `downloadJSON()`-Utility (kein neuer Code), exportiert `translations.de` als Vorlage — Übersetzer benennen die Datei beim Zurückladen auf ihren Sprachcode um. Settings-UI: neue "Sprache"-Sektion zwischen Icon-Pack und Einheiten (Sprachauswahl-Kacheln + Import-/Export-Buttons + Lösch-Liste für Community-Pakete, "DE" nicht löschbar). Live end-to-end im Browser getestet: eigenes Test-Sprachpaket (Spanisch, 2 Keys) hochgeladen, Sprache umgeschaltet — Topbar-Titel "Configuración" erschien sofort, unübersetzte Bereiche blieben korrekt auf Deutsch. 19 neue Checks in `test-suite.js`, 656/656 bestanden.
  - **Paket 5 Teil A (Einheiten/Formate-Fundament) ist damit komplett** — als nächstes Teil B (PDF-Baukasten 2.0, aus Phase 17), der große, mehrstufige Rewrite des PDF-Export-Systems.

*Teil B — PDF-Baukasten 2.0 (aus Phase 17, nutzt Teil A für z. B. den `table`-Block mit Checkpoint-Distanzen):*
- [x] `width`/`page_break_before`-Feld an `pdf_blocks` (Default `'full'`, rückwärtskompatibel) (19.08.2026)
- [x] `layoutBlocks()` Auto-Flow-Rendering + Tests (Breiten-Kombinationen, Seitenumbruch, Edge Case >100%) (19.08.2026)
- [ ] ~~`pdf_page_format` (A4/US Letter) im Event, dynamischer Satzspiegel in `export-pdf.js`, optionale Schnittmarken~~ — **beim Nachlesen der Spec-Dokumente entdeckt: das ist gar kein Teil-B-Punkt.** `ubergabe2.md` §20.2 führt „Papierformat-Adaption" explizit unter Phase 20 (= Teil A, Einheiten/Formate-Fundament), nicht unter Phase 17 — die terse Roadmap-Checkliste hatte es fälschlich unter die Teil-B-Überschrift gepackt. Bewusst NICHT im selben Zug wie der Baukasten gebaut: eine korrekte, seitenformat-relative Anpassung würde `exportManifestPDF()` betreffen, eine ~180-Zeilen-Funktion, die durchgehend mit absoluten, über viele frühere Phasen fein austarierten pt-Koordinaten arbeitet (nicht wie der Baukasten-Anhang-Code als Bruchteile von `pageWidth`/`pageHeight`) — ein „schnelles" Format-Umschalten ohne diese Koordinaten mit umzubauen würde bei US Letter (612×792pt vs. A4 595×842pt, 50pt weniger Höhe) real Inhalte am unteren Rand abschneiden können. Bleibt als eigener, separat zu planender Roadmap-Punkt offen statt es hier zu riskieren oder still zu unterschlagen. Siehe [[alleycat-roadmap]].
- [x] UI: Breiten-Dropdown + Seitenumbruch-Checkbox pro Block (19.08.2026)
- [x] Neue Blocktypen: `image` (mit Client-Komprimierung), `table` (App-Daten-Quellen), `variable_text` (Platzhalter-Interpolation) (19.08.2026)
- [x] Vorschau-Funktion (PDF im Hintergrund gerendert, als Bild angezeigt) (19.08.2026)
- [x] Dokument-Typ-Vorlagen (Default-Blocksets pro Dokumenttyp) + "Auf Standard zurücksetzen" (19.08.2026)
- [x] Vorlagen-Export/-Import um Breiten/Dokumenttyp erweitert (19.08.2026)
- [x] Regressionstest: bestehende Manifeste/Spokecards vor der Migration bleiben inhaltlich/optisch korrekt (19.08.2026)

**Umsetzung (19.08.2026):** Alle 7 genuinen Baukasten-Punkte in einem Zug gebaut und getestet (722/722 Tests, +66 seit Teil A). Wichtigste Entscheidungen:
- **`targetDocuments[]` statt `document_type`-Feld beibehalten.** Die Spec-eigene Datenmodell-Skizze (17.1) zeigt ein einzelnes `document_type`-Feld pro Block; das bestehende System aus Phase 14 nutzt aber schon ein flexibleres `targetDocuments: []`-Array (ein Block kann Manifest UND Spokecards gleichzeitig bedienen). Migration auf ein starres Einzelfeld wäre eine Funktionsregression gewesen — beibehalten, nur `width`/`pageBreakBefore` neu ergänzt.
- **Dokument-Typ-Vorlagen nur für `manifest`/`spokecards`.** Die Spec-Tabelle (17.6) nennt 4 Dokumenttypen inkl. `personal_briefing`/`aushang_endergebnis` — Phase 14 hatte aber schon bewusst festgelegt, dass `appendPdfBlocks()` nur für Manifest/Spokecards aufgerufen wird (Personal-Briefing/Startnummern bewusst ausgeschlossen), und ein „Aushang-Endergebnis"-Dokument existiert im Code überhaupt nicht. Diese Grenze aus Phase 14 unverändert übernommen statt einen komplett neuen PDF-Dokumenttyp zu bauen, den die Roadmap-Checkliste gar nicht verlangt.
- **`appendPdfBlocks()` grundlegend umgebaut** (`export-pdf.js`): `layoutBlocks()` gruppiert aktivierte Blöcke in Zeilen; jede Zeile bekommt weiterhin unbedingt eine eigene neue Seite (`doc.addPage()` pro Zeile, nicht pro Block) — das ist der Schlüssel zur Rückwärtskompatibilität: ein Alt-Event, dessen Blöcke alle `width:'full'` sind, erzeugt in `layoutBlocks()` immer eine Zeile pro Block (ein full-Block überschreitet beim übernächsten Block automatisch die 100%-Grenze), landet also weiterhin exakt wie vorher auf einer eigenen Seite. Erst echte neue Halb-/Drittel-Breiten-Blöcke teilen sich überhaupt eine Seite — dafür gibt es kein Alt-Verhalten, das brechen könnte. `pageBreakBefore` bleibt trotzdem sinnvoll: es entscheidet in `layoutBlocks()` die Zeilen-Zugehörigkeit (erzwingt eine neue Zeile trotz freiem Breiten-Budget), und jede erzwungene neue Zeile bekommt automatisch auch eine neue Seite. Einzelblock-Zeilen (der Normalfall: `width:'full'`) behalten die alte interne Mehrseiten-Paginierung bei sehr langem Inhalt (Checkpoint-Liste, langer Freitext); Mehrspalten-Zeilen (halb/drittel) nicht — ein Seitenumbruch mitten in einer Spalten-Zeile, der die Spalten synchron hält, ist genau die 2D-Grid-Editor-Komplexität, die 17.2 bewusst ausschließt (halb/drittel-Blöcke sind für kompakten Inhalt wie Logos/kleine Texte/Bilder gedacht, nicht für sprawling Listen).
- **Regressionstest** (17.8 Schritt 8) über `layoutBlocks()`-Form-Invariante + Seitenzahl-Assertion statt PDF-Byte-Vergleich: ein simulierter Alt-Block ohne `width`/`pageBreakBefore`-Feld bekommt nach der Migration `width:'full'` und erzeugt weiterhin exakt 1 eigene Seite — die eigentliche Garantie ist inhaltliche Korrektheit (nichts fehlt, nichts wird abgeschnitten), nicht eine buchstäblich unveränderte Seitenzahl bei mehreren kurzen full-width-Blöcken, da das Zusammenlegen mehrerer kurzer Zeilen auf einer Seite (wenn sie Platz haben) ein gewollter, positiver Nebeneffekt des Auto-Flow-Layouts ist.
- **`table`-Block-Quellen**: `checkpoint_distances` (nutzt `computeRouteLegs()` + Teil A's `formatDistance()`), `category_breakdown`, `team_list` — feste App-Daten-Renderer, kein Freiform-Tabellenbau (spec-konform).
- **`image`-Block**: Client-Komprimierung via Canvas (max. 1600px Breite, JPEG ~80%, kein Upscaling kleinerer Bilder) vor dem Speichern, `config:{dataUrl, imageDims, caption, alignment}`.
- **`variable_text`**: `{{event.name}}`/`{{event.date}}`/`{{event.riderCount}}`/`{{event.hqAddress}}`/`{{event.afterpartyName}}`, interpoliert erst beim Export (`interpolatePdfBlockVariables()`) — der gespeicherte Text bleibt unverändert, damit eine Vorlage über mehrere Events wiederverwendbar bleibt.
- **Vorschau-Funktion**: nutzt die bestehende Iframe-Vorschau aus Paket 1 (`showPdfPreview()`) statt einer neuen PDF→Bild-Rasterisierung — eine solche bräuchte eine neue Abhängigkeit (z. B. pdf.js), was die "keine neuen Dependencies"-Regel verletzen würde, und eine echte PDF-Vorschau im Iframe ist ohnehin die bessere UX als ein statisches Bild. Baut ein eigenständiges Mini-Dokument nur aus den Baukasten-Blöcken des gewählten Dokumenttyps (nicht das volle Manifest), damit die Vorschau schnell bleibt und nicht bei jedem Tastendruck neu rendert (17.3's explizite Anforderung).
- **Neue Events bekommen jetzt vorbefüllte Dokument-Vorlagen** (`defaultPdfBlocksForNewEvent()`, aufgerufen aus `createNewEvent()`, nicht aus `withEventDefaults()` — damit bestehende Events beim nächsten Laden keine überraschenden neuen Blöcke bekommen): 5 Manifest- + 2 Spokecards-Default-Blöcke statt eines leeren Baukastens.

### Paket 6 — Spezielle Rennformate (Phase 21)

- [ ] ~~Cargo-Modul: `is_cargo_event`, `cp.cargo_item` (Name/Gewicht/Volumen/Bonuspunkte), `rider.cargo_capacity_kg`, Punkteberechnung im Ziel-Check-in (andockend an `points_ledger` aus Phase 11)~~ — **gebaut (19.08.2026), auf ausdrücklichen Wunsch wieder entfernt (19.08.2026).** Kein technischer Grund, reine Scope-Entscheidung des Nutzers.
- [ ] ~~Trackbike-Attribute: `gear_ratio`, `is_brakeless`, `is_workbike` + Leaderboard-Filter/Badges ("Fastest Brakeless", "Best Workbike")~~ — **gebaut (19.08.2026), auf ausdrücklichen Wunsch wieder entfernt (19.08.2026).** Ebenfalls reine Scope-Entscheidung, kein technischer Grund.
- [x] PDF-Block `clue_sheet` (nutzt das in Paket 5 erweiterte Block-System) (19.08.2026) — **einziger Punkt aus diesem Paket, der steht.**

**Umsetzung (19.08.2026):** Ursprünglich alle 3 Punkte in einem Zug gebaut (neues Modul `src/core/race-formats.js`, 784/784 Tests), danach auf expliziten Wunsch des Nutzers Cargo-Modul und Trackbike-Attribute vollständig zurückgebaut — Datenmodell-Felder (`cp.cargoItem`, `rider.cargoCapacityKg`/`gearRatio`/`isBrakeless`/`isWorkbike`), UI (Checkpoint-Edit-Form, Fahrerkarten, Ziel-Check-in-Fracht-Sektion, Leaderboard-Filter/Badge), die beiden Feature-Registry-Einträge (`cargo_module`/`trackbike_attributes`), zugehörige i18n-Strings und Tests — alles rückstandslos entfernt, verifiziert per Grep (keine Restfunde außer erklärenden Kommentaren) und vollem Testlauf (733/734, gleicher bekannter Zeitflake wie immer). `src/core/race-formats.js` bleibt bestehen, enthält jetzt nur noch die Clue-Sheet-Chiffre-Helfer.
- **Rätsel-Sheet-Chiffre ist bewusst keine echte Kryptographie**, sondern eine von Hand entschlüsselbare Ziffern-Verschiebung (`clueSheetCipherDigits()`: jede Ziffer +N mod 10, Nicht-Ziffern unverändert) — passend zum Charakter eines analogen Papier-Rätsels für ein Alleycat, nicht ein Sicherheits-Feature. Der Schlüssel N (1–9, pro Block konfigurierbar) wird als Legende mit auf dem Sheet gedruckt, damit Fahrer:innen von Hand zurückrechnen können. Nutzt bewusst das bereits vorhandene `cp.clue`-Feld als "Straßennetz-Hinweis" statt ein neues Datenfeld einzuführen — das ist exakt, wofür `clue` schon da ist. Leere Stempelfelder werden als `doc.rect()`-Rahmen gezeichnet (kein neues Bild-Asset nötig), optional per Checkbox abschaltbar.
- Der Block-Typ reiht sich nahtlos in das in Paket 5 gebaute Auto-Flow-System ein (`width`/`pageBreakBefore`, eigene Vorschau) — keine Sonderbehandlung nötig, da `renderPdfBlockColumn()`s bestehendes `pagination`-Fallback-Muster (Einzelblock-Zeilen paginieren bei langem Inhalt automatisch weiter) direkt wiederverwendet wird.

### Paket 7 — Beamer-Erweiterungen (Phase 23)

**Übersprungen (19.08.2026), auf Nutzerwunsch — kein Bedarf.** Nicht gebaut, aus der aktiven Roadmap raus.

- [ ] ~~Event-Ticker-Infrastruktur bereits vorhanden (`pushEventLog()`, Phase 12) — liefert die Ereignisse, die vertont werden sollen~~
- [ ] ~~`speechSynthesis`-Integration: Beamer-Settings-Toggle, Stimme/Lautstärke/Geschwindigkeit einstellbar~~
- [ ] ~~`SpeechRecognition`-Integration: Voice-Check-in im Ziel-Marshal-Flow ("Startnummer 42 Ziel")~~

### Paket 8 — Paket-Abholung/-Zustellung (nicht Teil der ursprünglichen Übergabe-Dokumente)

Ad-hoc-Feature-Wunsch (19.08.2026, nach Paket 7 übersprungen): zwei neue, verknüpfte Checkpoint-Typen, damit eine Route eine Kurier-Aufgabe abbilden kann — Fahrer holt an Checkpoint A ein Paket ab und muss es an Checkpoint B zustellen.

- [x] Zwei neue `CHECKPOINT_TYPES`-Einträge: `pickup` (📤 Paket-Abholung) und `dropoff` (📥 Paket-Zustellung) (19.08.2026)
- [x] Verknüpfung (`cp.pairedDropoffCpId`, gesetzt am Abholung-Checkpoint) + Karten-Editor-UI (Dropdown am Abholung-CP, Read-only-Anzeige am Zustell-CP, Sidebar-Badges in beide Richtungen) (19.08.2026)
- [x] Ziel-Check-in-Gate: Zustellung ohne bestätigte Abholung fragt nach (Confirm+Override-Muster wie beim bestehenden `checkpointOrderMode:'fest'`), plus Status-Hinweise pro Checkpoint-Zeile ("Abholung noch offen"/"Abholung erledigt"/Ziel-Hinweis am Abholung-CP) (19.08.2026)
- [x] Aufräumen bei Löschung: Löschen des Zustell-Checkpoints entfernt die Verknüpfung beim Abholung-Checkpoint automatisch (kein Dangling Reference) (19.08.2026)

**Umsetzung (19.08.2026):** 755/755 Tests (+21). Bewusst **kein** eigenes Cargo-Datenmodell (Gewicht/Volumen/Bonuspunkte, Kapazitäts-Tracking) — das war exakt das gerade erst wieder entfernte Cargo-Modul aus Paket 6. Diesmal minimal gehalten: zwei einfache, gepaarte Checkpoint-Typen nach demselben Muster wie die bestehenden 5 (`qr`/`photo`/`item`/`custom`/`challenge`) — kein neues Datenmodell außer der einen Verknüpfungs-ID, keine Feature-Registry-Gate (anders als Cargo/Trackbike: Checkpoint-Typen sind schon immer ungated wählbar, ein neuer Typ braucht keinen extra Ein/Aus-Schalter). Verknüpfung wird bewusst nur auf dem Abholung-Checkpoint gespeichert (`pairedDropoffCpId`), der Zustell-Checkpoint sucht rückwärts über `pickupForDropoff()` — vermeidet eine zweite, redundante Referenz, die aus dem Gleichschritt geraten könnte. Das Confirm-und-Override-Verhalten im Ziel-Check-in kopiert bewusst exakt das bestehende Muster von `checkOrderBeforeComplete()` (fest-Reihenfolge-Warnung) statt hart zu blockieren — ein Marshal kann eine vergessene Abholung am Zustell-Checkpoint nachträglich bestätigen, ohne technisch blockiert zu sein.

### Paket 9 — Settings-Sidebar-Redesign (nicht Teil der ursprünglichen Übergabe-Dokumente)

Ad-hoc-UX-Wunsch (19.08.2026): die Settings-Seite war eine einzige lange Scroll-Seite mit 9 untereinander gestapelten Abschnitten (Feature-Registry, Theme, Icon-Pack, Sprache, Einheiten, Checkpoint-Typen, Datensicherheit inkl. Backup/Speicher/Offline). Umgebaut auf eine Obsidian-artige Zwei-Spalten-Ansicht: linke Sidebar mit gruppierter Navigation, rechts nur der aktive Screen.

- [x] Sidebar mit 3 Gruppen/7 Einträgen: **Allgemein** (Feature-Übersicht/Design/Icon-Pack/Sprache/Einheiten), **Event** (Checkpoint-Typen), **Daten & Sicherheit** (Datensicherheit — Backup/Speicher/Offline-Bereitschaft bleiben als Unterabschnitte einer Seite gebündelt, nicht in 3 einzelne Nav-Punkte aufgesplittet) (19.08.2026)
- [x] Aktiver Abschnitt geräteweit gemerkt (`localStorage['alleycat:settingsSection']`, gleiches Muster wie `alleycat:sidebarCollapsed`) — Settings öffnet beim nächsten Mal wieder im zuletzt gewählten Abschnitt (19.08.2026)
- [x] Mobile Drill-down unterhalb desselben Breakpoints wie der Editor (`SIDEBAR_BREAKPOINT`, 820px): erst nur die Liste, Tap zeigt den gewählten Screen vollflächig mit "← Übersicht"-Zurück-Button; oberhalb des Breakpoints klassische Sidebar+Content nebeneinander (19.08.2026)
- [x] `jumpToFeatureConfig('offline-settings')` (Feature-Registry-Zahnrad-Button) wählt jetzt `state.settingsSection = 'dataSafety'` statt nur zu scrollen — die anderen 3 `configScreen`-Ziele (Sound-Hook/Kategorien/Spielmodi) navigieren wie schon vorher zu Übersicht/Fahrer-Ansicht, nicht in die Settings, daher unverändert (19.08.2026)

**Umsetzung (19.08.2026):** 771/771 Tests (+16, davon 4 bestehende Settings-Tests angepasst, die vorher naiv den kompletten `#view-settings`-Seiteninhalt nach allen Überschriften gleichzeitig durchsucht hatten — nach dem Umbau ist immer nur der aktive Screen im DOM). Bewusst **kein** 1:1-Umbau auf "jede `<h3>`-Überschrift wird ein eigener Nav-Punkt" — Backup/Speicher/Offline-Bereitschaft bleiben als eine zusammenhängende "Datensicherheit"-Seite mit mehreren Unterabschnitten (spart einen Umbau von `data-safety.js`/`offline-tiles.js`, deren Render-Funktionen bereits ineinandergreifen, für einen Komfortgewinn, der bei nur 3 Einträgen in einer Gruppe gering gewesen wäre). Sidebar-Nav-Labels als `() => t(...)`-Funktionen (nicht als am Ladezeitpunkt ausgewertete Strings wie bei `THEMES`) — derselbe Grund wie bei `FEATURE_REGISTRY`: ein nachträglich geladenes Community-Sprachpaket muss den Text bei jedem `render()` neu übersetzen können.

### Paket 10 — Auto-Backup an/ausschaltbar (nicht Teil der ursprünglichen Übergabe-Dokumente)

Ad-hoc-Nutzerwunsch (19.08.2026): Auto-Backup lief bisher immer automatisch mit, sobald ein Rennen den Status "Läuft" hatte — keine Möglichkeit, es abzuschalten. Jetzt ein eigener Ein/Aus-Schalter in den Einstellungen, **Default: aus**.

- [x] `state.appSettings.autoBackupEnabled` (device-lokal wie Theme/Icon-Pack/Intervall), Default `false` (19.08.2026)
- [x] `runAutoBackupTick()` bricht sofort ab, wenn deaktiviert — noch vor der bestehenden "läuft ein Rennen?"-Prüfung (19.08.2026)
- [x] Toggle-Switch in der Datensicherheit-Sektion (wiederverwendet dieselbe `.toggle-switch`-Komponente wie die Feature-Registry); das Intervall-Feld wird nur angezeigt, wenn aktiviert (19.08.2026)

**Umsetzung (19.08.2026):** 777/777 Tests (+6). Der manuelle "Jetzt sichern"-Button bleibt bewusst unabhängig vom Schalter immer verfügbar — der Schalter steuert nur den automatischen Intervall-Download, nicht die manuelle Aktion. Bestehende Nutzer mit einer schon gespeicherten `appSettings`-Blob ohne dieses Feld bekommen automatisch den neuen Default (`false`) über den bestehenden `Object.assign(default, gespeichert)`-Merge in `loadAppSettings()` — kein Migrations-Code nötig.

### Paket 11 — Fahrer-Sidebar-Redesign (nicht Teil der ursprünglichen Übergabe-Dokumente)

Ad-hoc-UX-Wunsch (19.08.2026), direkte Fortsetzung von Paket 9: die Fahrer-Seite hatte denselben "alles stapelt sich vertikal"-Charakter wie die alte Settings-Seite — 8+ Buttons in einer Toolbar-Zeile, bis zu drei aufklappbare Panels (Teams/Kategorien/Kartendesign) darüber gestapelt, der eigentliche Fahrer-Roster erst ganz unten. Anders als bei Settings entschied sich der Nutzer hier explizit für das **volle Settings-Muster** (Sidebar ersetzt den kompletten Content, "Fahrerliste" ist nur ein Punkt von mehreren) statt einer Master-Detail-Variante mit dauerhaft sichtbarem Roster — plus zwei zusätzliche UX-Verbesserungen (Suche, Sortierung/Gruppierung), die er bei derselben Abfrage vorschlug.

- [x] Sidebar mit 2 Gruppen/5 Einträgen: **Roster** (Fahrerliste, CSV-Import), **Konfiguration** (Teams, Kategorien — nur wenn Feature aktiv, Kartendesign) (19.08.2026)
- [x] `openRiders()` setzt die Sektion bei jedem Aufruf auf `'roster'` zurück — bewusst **kein** "letzten Abschnitt merken" wie bei Settings: die Fahrerliste ist der mit Abstand dominante Grund, warum diese Seite überhaupt besucht wird, ein zufälliges Landen auf "Kartendesign" von der letzten Sitzung wäre schlechtere UX, nicht bessere (19.08.2026)
- [x] Identisches Mobile-Drill-down wie Settings (gleicher `SIDEBAR_BREAKPOINT`, gleiche `.settings-layout`/`.settings-sidebar`/`.settings-content`/`.settings-mobile-*`-Klassen wiederverwendet statt neu erfunden — rein strukturelle Klassen ohne Settings-spezifische Kopplung) (19.08.2026)
- [x] `jumpToFeatureConfig('category-settings')` navigiert jetzt direkt zur Kategorien-Sektion (`selectRidersSection('categories')`) statt nur ein `categoriesPanelOpen`-Flag zu setzen (19.08.2026)
- [x] Roster-Suche (Name oder Startnummer, live-Filter) (19.08.2026)
- [x] Sortierung/Gruppierung: Startnummer (Standard) / Name / Team — bei "Team" echte Gruppierung mit Überschriften (`.rider-group-heading`, analog zum bestehenden Checkpoint-Gruppierungsmuster), nicht nur eine andere Sortierreihenfolge. Gruppierung nach Kategorie bewusst nicht angeboten — ein Event kann mehrere unabhängige Kategorie-Gruppen haben (Antrieb, Gender, …), "nach Kategorie gruppieren" hätte keine eindeutige Achse (19.08.2026)

**Umsetzung (19.08.2026):** 794/794 Tests (+17, davon 2 bestehende Settings-Tests repariert — sie nutzten einen ungescopten `document.querySelector('.settings-content')`, der durch die Wiederverwendung derselben Klasse auf der Fahrer-Seite plötzlich mehrdeutig wurde und je nach DOM-Reihenfolge die falsche Seite traf; gefixt durch Scoping auf `#view-settings .settings-content`). Suche/Sortierung wirken bewusst auch auf das, was gedruckt wird (`#print-root` umschließt genau das gefilterte/sortierte/gruppierte Grid) — "drucke was du siehst" entspricht dem Verhalten, das man von einer gefilterten Tabelle erwartet, und Team-Gruppen-Überschriften mitzudrucken ist ein Feature (leichteres physisches Sortieren am Renntag), kein Bug. Die Undo-Log-Anzeige (`renderActionLogPanel`) bleibt bewusst nur im Roster-Abschnitt, nicht auf jeder Sektion dupliziert.

---

## 4. Zurückgestellte Ideen (nicht Teil der aktiven Roadmap)

### Offline-Gerätesync (Phase 22, Screen-to-Camera Optical Sync)

Vorerst nur Idee, kein aktives Arbeitspaket — Entscheidung vom 17.08.2026. Einzige Phase mit unsicherem Aufwand, adressiert einen Nischen-Anwendungsfall (Checkpoints ganz ohne Mobilfunknetz). Steht im selben "vertagt/offen"-Bereich wie Kopfgeld-Modus oder Service-Worker aus der ursprünglichen Übergabe — wird erst wieder aufgegriffen, wenn konkreter Bedarf entsteht.

- [x] Kamera-Zugriff + QR-Decoding-Grundlage bereits vorhanden (`jsQR`, bestehender QR-Scan-Checkpoint-Typ in `checkin.js`) — falls die Idee später aktiviert wird, muss hier nicht bei null gestartet werden
- [ ] Sender: animierter Multi-Frame-QR-Code-Stream (JSON-Paket-Encoding über mehrere Frames)
- [ ] Empfänger: Webcam-Loop-Decoding mehrerer Frames zu vollständigem Datensatz, Einfügen in HQ-DB

---

## 5. Offene Rückfrage

**Kapazität für Parallel-Spuren:** Paket 3 (Backend) berührt keine Datei, die die anderen Pakete anfassen — lohnt es sich, es wirklich parallel zu Paket 1/2 zu bauen, oder bleibt strikt sequenziell (ein Paket nach dem anderen) einfacher zu verfolgen? Bisher unentschieden, aber nicht blockierend für den Start von Paket 1.
